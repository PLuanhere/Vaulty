<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Màu nền ở đây -->
    <solid android:color="#ECE9E9"/>
    <!-- Viền ở đây -->
    <stroke
        android:width="4dp"
        android:color="#22646A"/>
    <corners android:radius="24dp"/>
</shape>